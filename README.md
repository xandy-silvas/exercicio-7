7. Inversão de string

   Crie uma função recursiva em C que inverta uma string.  

   Exemplo:

   Entrada: `"recursão"` → Saída: `"oãsrucer"` (assumindo tratamento de acentuação).  

   Observação: Modifique a string original, não crie uma nova.
